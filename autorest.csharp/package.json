{
  "name": "@cuteribs/autorest.csharp",
  "version": "2.4.3",
  "description": "The C# extension for classic generators in AutoRest.",
  "scripts": {
    "start": "dotnet bin/autorest.csharp.dll --server",
    "build": "dotnet publish src -f net8.0 -c Release -o bin -p:Version=$(npm pkg get version)",
    "publish": "npm run build && npm publish --access public"
  }
}
