{
  "name": "@cuteribs/autorest.typescript",
  "version": "5.1.0",
  "description": "The typescript extension for classic generators in AutoRest.",
  "scripts": {
    "start": "dotnet bin/autorest.typescript.dll --server",
    "build": "dotnet publish src -f net6.0 -c Release -o bin",
    "publish": "npm run build && npm publish --access public"
  },
  "devDependencies": {},
  "dependencies": {}
}
